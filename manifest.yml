---
applications:
- name: figpulse
  memory: 512M
  instances: 1
  command: bundle exec rake db:migrate && env PROJECT_MONITOR_LOGIN=profburke PROJECT_MONITOR_EMAIL=djanssen@pivotal.io PROJECT_MONITOR_PASSWORD=jk456simmons bundle exec rake db:seed  && cat /home/vcap/app/farmers/figpulse.configuration.yml | bundle exec rake projectmonitor:import  && bundle exec unicorn -p $PORT -c ./config/unicorn.rb 
